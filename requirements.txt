flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
```

**What these are:**
- **flask** - Web framework (backend server)
- **flask-cors** - Allows frontend to talk to backend
- **python-dotenv** - Load environment variables

---

## **SAVE requirements.txt**

---

## **STEP 5: CREATE .env.example (1 MIN)**

**Open `.env.example` and add:**
```
# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True
PORT=5000
```

---

## **SAVE .env.example**

---

## **STEP 6: CREATE .gitignore (1 MIN)**

**Open `.gitignore` and add:**
```
# Environment
.env

# Python
__pycache__/
*.py[cod]
*.so

# Flask
instance/
.pytest_cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db